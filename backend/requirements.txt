Django==5.0.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
psycopg2-binary==2.9.10
Pillow==10.4.0
python-decouple==3.8
django-filter==24.2
django-anymail[resend]
gunicorn==22.0.0
whitenoise==6.12.0

# Optional: keep Django-Admin uploads in S3-compatible object storage
# (Cloudflare R2 / Backblaze B2 / AWS S3), which is the only way they survive a
# deploy on Render's ephemeral filesystem. Uncomment, then set USE_S3=True plus
# the AWS_* variables on the service.
# django-storages[s3]==1.14.6

